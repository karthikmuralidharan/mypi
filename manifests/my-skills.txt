# Skills I authored myself, living in ~/.pi/agent/skills/.
# Single source of truth for sync.sh (pull) and bootstrap.sh (push).
# Everything else in ~/.pi/agent/skills/ is plugin-installed and
# regenerated by `pi install` — do not list those here.
atlassian-rest-cli
ce-rigor
chrome-cdp-drive-sync
eng-loop-jira-gh-sync-tooling
graphify
loop
rebuild-pi-native-modules
